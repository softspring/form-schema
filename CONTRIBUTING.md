# Contributing

This package is in active development. Keep changes small and include tests for extractor behavior.

Use the standard package commands before sending changes:

```bash
composer fix
composer test
composer test-bc
```

[Report issues](https://github.com/softspring/form-schema/issues) and [send Pull Requests](https://github.com/softspring/form-schema/pulls)
